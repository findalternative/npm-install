name: npm-install

on:
  push:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: testing
        run: cd baserepo
      - name: testing
        working-directory: ./baserepo
        run: ls
